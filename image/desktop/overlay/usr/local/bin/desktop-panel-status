#!/bin/bash
# desktop-panel-status - text for the tint2 status slot.
#
# Rook shows nothing here. Setup is prompted in the welcome terminal (the
# rook-greeting banner says "run rook config" until a key is set), so a panel
# label would only be decorative. Emitting no output leaves the slot empty and
# suppresses any base default.

exit 0
